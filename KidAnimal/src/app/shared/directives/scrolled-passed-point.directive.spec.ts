import { ScrolledPassedPointDirective } from './scrolled-passed-point.directive';

describe('ScrolledPassedPointDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrolledPassedPointDirective();
    expect(directive).toBeTruthy();
  });
});
